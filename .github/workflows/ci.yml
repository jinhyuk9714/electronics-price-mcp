name: CI

on:
  push:
    branches:
      - main
      - 'codex/**'
  pull_request:
    branches:
      - main
      - 'codex/**'

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Run deterministic CI verification
        run: npm run verify:ci

      - name: Upload deterministic evaluation reports
        uses: actions/upload-artifact@v4
        with:
          name: deterministic-evaluation-reports
          path: |
            reports/multisource-merge-latest.json
            reports/multisource-merge-latest.md
            reports/static-canary-service-quality-100-latest.json
            reports/static-canary-service-quality-100-latest.md
            reports/static-canary-service-quality-advanced-100-latest.json
            reports/static-canary-service-quality-advanced-100-latest.md
          if-no-files-found: error
